
export default function renderCountry(country){

    const arr = country.then(arr => arr);
    console.log(arr);
    if(arr.length > 10){
        console.log(arr.length);
    } 
    if(arr.length <= 10){
        
    }
    if(arr.length === 1){
        
    }
}